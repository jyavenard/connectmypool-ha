{
  "username": "usernam@domain.org",
  "password": "PUT-PASSWORD-HERE-OR-USE-CMP_PASS-ENV",
  "pool_name": "JYA Pool",

  "_comment_calibration": "The ORP set point is read as text from Chemistry.aspx and the chart y-axis is OCR'd (needs the tesseract binary), so neither depends on the values below. Those are ONLY the degraded fallback used when tesseract is unavailable: orp_units_per_gridline cannot be derived at runtime and the axis switches between 10 and 20, so install tesseract instead of tuning them.",
  "tesseract_cmd": null,
  "orp_units_per_gridline": 10,
  "orp_setpoint": 690,
  "orp_axis_top": 710,

  "json_out": "/var/www/html/pool.json",
  "debug_dir": "/tmp/cmp_debug",
  "retries": 3
}
